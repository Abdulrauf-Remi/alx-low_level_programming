#include "main.h"

/**
 * _strlen - Return the length of a string.
 * @str: The string in question.
 *
 * Return: The lenght of str.
 */
int _strlen(char *s)
{
	int len = 0;

	while (*str++)
		len++;

	return (len);
}
